# vim: set ft=sh:
run_hook ()
{
    hl "   :: 正在载入 udev..."
    echo > /proc/sys/kernel/hotplug
    /sbin/udevd --daemon 2>/dev/null 1>/dev/null
    /sbin/udevadm trigger 2>/dev/null 1>/dev/null
    /sbin/udevadm settle 2>/dev/null 1>/dev/null
    hl "   完成.\n"
}
